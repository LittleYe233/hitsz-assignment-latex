\documentclass{assignment}
% \documentclass[anonymous]{assignment}

\author{鸣瀬咲}
\homework{恋愛になれていない\ 気象予報士}
\stuno{20171124}
\stumajor{Making＊Lovers}
\portrait{images/landscape.png}

% Document body
\begin{document}
    \maketitle

    \chapter{练习 1}

    % Problem 1-1
    \solveprob{1-1}{
        $114,514.$
    }{
        \textbf{(1)}

        测试文字. $\text{Not implemented.}$\ 测试文字. $\text{Not implemented.}$
        
        $$
        \sum\limits_{i=1}^{n}P(A_i)=P(\sum\limits_{i=1}^{n}A_i).
        $$

        测试文字. $\text{Not implemented.}$\ 测试文字. $\text{Not implemented.}$
    }
    
    % Problem 1-2
    \proveprob{1.}{
        测试 test.

        $\arc{abc}$.
    }

    \chapter{练习 2}

    \solveprob{1.}{}{
        \textbf{(1)}

        $\dfrac{1}{3+2i}=\dfrac{3-2i}{(3+2i)(3-2i)}=\dfrac{3-2i}{13}$.

        模长为 $\dfrac{\sqrt{13}}{13}$, 辐角为 $\arctan\left(-\dfrac{2}{3}\right)+2k\pi(k\in\mathbb{Z})$.

        \textbf{(2)}

        $\dfrac{1}{i}-\dfrac{3i}{1-i}=\dfrac{3-5i}{2}$.

        模长为 $\dfrac{\sqrt{34}}{2}$, 辐角为 $\arctan\left(-\dfrac{5}{3}\right)+2k\pi(k\in\mathbb{Z})$.
    }

    \solveprob{2.}{}{
        若题设等式成立, 则有

        $$
        x+1+(y-3)i=2+8i.
        $$

        则

        $$
        \begin{cases}
            x+1=2\\
            y-3=8.
        \end{cases}
        $$

        解得 $x=1,y=11$.
    }
\end{document}
